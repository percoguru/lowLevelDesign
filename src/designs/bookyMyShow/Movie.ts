class Movie {
  id: number;
  name: string;
  duration: number;

  constructor(name: string, id: number, duration: number) {
    this.name = name;
    this.id = id;
    this.duration = duration;
  }
}
